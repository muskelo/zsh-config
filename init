ZDOTDIR=~/.config/zsh

# import oh_my_zsh config
if [[ -f "$ZDOTDIR/oh_my_zsh" ]]; then
    source "$ZDOTDIR/oh_my_zsh";
fi

# import env vars
if [[ -f "$ZDOTDIR/env" ]]; then
    source "$ZDOTDIR/env";
fi

# import aliases 
if [[ -f "$ZDOTDIR/aliases" ]]; then
    source "$ZDOTDIR/aliases";
fi
